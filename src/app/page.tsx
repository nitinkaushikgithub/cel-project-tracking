// Placeholder home page. Login, roles, and the dashboard are build-order
// steps 2 and 7 (CLAUDE.md §8) — not implemented yet. This page exists only
// so the app has something to build and serve while the Docker Compose
// stack and Prisma schema are reviewed.
export default function Home() {
  return (
    <main>
      <h1>CEL Project Monitoring</h1>
      <p>Setup in progress — screens are not built yet.</p>
    </main>
  );
}
